<%= form_for(@say) do |f| %>
  <% if @say.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@say.errors.count, "error") %> prohibited this say from being saved:</h2>

      <ul>
      <% @say.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nickname %><br />
    <%= f.text_field :nickname %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
